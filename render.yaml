services:
  - type: web
    name: hrms-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_FRONTEND_ONLY
        value: "true"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
